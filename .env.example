PORT=5000
MONGO_URI=__url
JWT_SECRET=change_this_to_a_long_random_secret

# Optional (only if you want email password reset to actually send)
APP_URL=http://localhost:5173
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=
